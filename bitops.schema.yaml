aws:
  type: object
  properties:
    cli:
      type: object
      properties:
        aws-region:
          type: string
          default: us-east-1
          export_env: BITOPS_AWS_DEFAULT_REGION
